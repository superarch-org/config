# Loader: keep ~/.config/tmux minimal.
# Load superarch defaults first, then user overrides.
if-shell 'test -f ~/.config/superarch/tmux/tmux.conf' 'source-file ~/.config/superarch/tmux/tmux.conf'
if-shell 'test -f ~/.config/custom/tmux/tmux.conf'   'source-file ~/.config/custom/tmux/tmux.conf'
